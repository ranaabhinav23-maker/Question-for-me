name = "mymedicinefriend"
main = "worker.js"
compatibility_date = "2024-01-01"
```

---

## Add your secrets in Cloudflare

Go to your Worker → **Settings → Variables and Secrets** → add both:

| Name | Value |
|---|---|
| `ANTHROPIC_API_KEY` | `sk-ant-...` |
| `NOTION_TOKEN` | `secret_...` |

---

## Your repo structure should now be
```
public/index.html
worker.js          ← replace with new file above
wrangler.toml      ← add this new file
vercel.json        ← can delete this
api/generate.js    ← can delete this
